============================================================
 فایل: AuthController.php (پوشه: app\Http\Controllers\Api)
============================================================

تعداد نسخه‌ها: 7

----------------------------------------
 نسخه 1 : AuthController (2).php
----------------------------------------
<?php
namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

class AuthController extends Controller
{
 public function login(Request \)
 {
  \ = \->validate([
   'email'=>'required|email',
   'password'=>'required'
  ]);

  if(!auth()->attempt(\)){
   return response()->json(['message'=>'Invalid credentials'],401);
  }

  \ = auth()->user()->createToken('api-token')->plainTextToken;

  return response()->json([
   'token'=>\,
   'user'=>auth()->user()
  ]);
 }

 public function logout(Request \)
 {
  \->user()->tokens()->delete();
  return response()->json(['message'=>'Logged out']);
 }
}


----------------------------------------
 نسخه 2 : AuthController (3).php
----------------------------------------
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\Hash;
use App\Models\User;

class AuthController extends Controller
{
    public function login(Request $request)
    {
        $credentials = $request->validate([
            "email" => "required|email",
            "password" => "required"
        ]);

        if (!Auth::attempt($credentials)) {
            return response()->json([
                "message" => "Ø§ÛŒÙ…ÛŒÙ„ ÛŒØ§ Ø±Ù…Ø² Ø¹Ø¨ÙˆØ± Ø§Ø´ØªØ¨Ø§Ù‡ Ø§Ø³Øª"
            ], 401);
        }

        $user = Auth::user();
        $token = $user->createToken("api-token")->plainTextToken;

        return response()->json([
            "message" => "ÙˆØ±ÙˆØ¯ Ù…ÙˆÙÙ‚ÛŒØªâ€ŒØ¢Ù…ÛŒØ²",
            "token" => $token,
            "user" => [
                "id" => $user->id,
                "name" => $user->name,
                "email" => $user->email,
                "role" => $user->role ?? "technician"
            ]
        ]);
    }

    public function logout(Request $request)
    {
        $request->user()->currentAccessToken()->delete();
        
        return response()->json([
            "message" => "Ø®Ø±ÙˆØ¬ Ù…ÙˆÙÙ‚ÛŒØªâ€ŒØ¢Ù…ÛŒØ²"
        ]);
    }

    public function me(Request $request)
    {
        return response()->json([
            "user" => $request->user()
        ]);
    }
}


----------------------------------------
 نسخه 3 : AuthController (4).php
----------------------------------------
<?php
namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

class AuthController extends Controller
{
 public function login(Request \)
 {
  \ = \->validate([
   'email'=>'required|email',
   'password'=>'required'
  ]);

  if(!auth()->attempt(\)){
   return response()->json(['message'=>'Invalid credentials'],401);
  }

  \ = auth()->user()->createToken('api-token')->plainTextToken;

  return response()->json([
   'token'=>\,
   'user'=>auth()->user()
  ]);
 }

 public function logout(Request \)
 {
  \->user()->tokens()->delete();
  return response()->json(['message'=>'Logged out']);
 }
}


----------------------------------------
 نسخه 4 : AuthController (5).php
----------------------------------------
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\Hash;
use App\Models\User;

class AuthController extends Controller
{
    public function login(Request $request)
    {
        $credentials = $request->validate([
            "email" => "required|email",
            "password" => "required"
        ]);

        if (!Auth::attempt($credentials)) {
            return response()->json([
                "message" => "Ø§ÛŒÙ…ÛŒÙ„ ÛŒØ§ Ø±Ù…Ø² Ø¹Ø¨ÙˆØ± Ø§Ø´ØªØ¨Ø§Ù‡ Ø§Ø³Øª"
            ], 401);
        }

        $user = Auth::user();
        $token = $user->createToken("api-token")->plainTextToken;

        return response()->json([
            "message" => "ÙˆØ±ÙˆØ¯ Ù…ÙˆÙÙ‚ÛŒØªâ€ŒØ¢Ù…ÛŒØ²",
            "token" => $token,
            "user" => [
                "id" => $user->id,
                "name" => $user->name,
                "email" => $user->email,
                "role" => $user->role ?? "technician"
            ]
        ]);
    }

    public function logout(Request $request)
    {
        $request->user()->currentAccessToken()->delete();
        
        return response()->json([
            "message" => "Ø®Ø±ÙˆØ¬ Ù…ÙˆÙÙ‚ÛŒØªâ€ŒØ¢Ù…ÛŒØ²"
        ]);
    }

    public function me(Request $request)
    {
        return response()->json([
            "user" => $request->user()
        ]);
    }
}


----------------------------------------
 نسخه 5 : AuthController (6).php
----------------------------------------
<?php
namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

class AuthController extends Controller
{
 public function login(Request \)
 {
  \ = \->validate([
   'email'=>'required|email',
   'password'=>'required'
  ]);

  if(!auth()->attempt(\)){
   return response()->json(['message'=>'Invalid credentials'],401);
  }

  \ = auth()->user()->createToken('api-token')->plainTextToken;

  return response()->json([
   'token'=>\,
   'user'=>auth()->user()
  ]);
 }

 public function logout(Request \)
 {
  \->user()->tokens()->delete();
  return response()->json(['message'=>'Logged out']);
 }
}


----------------------------------------
 نسخه 6 : AuthController (7).php
----------------------------------------
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\Hash;
use App\Models\User;

class AuthController extends Controller
{
    public function login(Request $request)
    {
        $credentials = $request->validate([
            "email" => "required|email",
            "password" => "required"
        ]);

        if (!Auth::attempt($credentials)) {
            return response()->json([
                "message" => "Ø§ÛŒÙ…ÛŒÙ„ ÛŒØ§ Ø±Ù…Ø² Ø¹Ø¨ÙˆØ± Ø§Ø´ØªØ¨Ø§Ù‡ Ø§Ø³Øª"
            ], 401);
        }

        $user = Auth::user();
        $token = $user->createToken("api-token")->plainTextToken;

        return response()->json([
            "message" => "ÙˆØ±ÙˆØ¯ Ù…ÙˆÙÙ‚ÛŒØªâ€ŒØ¢Ù…ÛŒØ²",
            "token" => $token,
            "user" => [
                "id" => $user->id,
                "name" => $user->name,
                "email" => $user->email,
                "role" => $user->role ?? "technician"
            ]
        ]);
    }

    public function logout(Request $request)
    {
        $request->user()->currentAccessToken()->delete();
        
        return response()->json([
            "message" => "Ø®Ø±ÙˆØ¬ Ù…ÙˆÙÙ‚ÛŒØªâ€ŒØ¢Ù…ÛŒØ²"
        ]);
    }

    public function me(Request $request)
    {
        return response()->json([
            "user" => $request->user()
        ]);
    }
}


----------------------------------------
 نسخه 7 : AuthController.php
----------------------------------------
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\Hash;
use App\Models\User;

class AuthController extends Controller
{
    public function login(Request $request)
    {
        $credentials = $request->validate([
            "email" => "required|email",
            "password" => "required"
        ]);

        if (!Auth::attempt($credentials)) {
            return response()->json([
                "message" => "Ã˜Â§Ã›Å’Ã™â€¦Ã›Å’Ã™â€ž Ã›Å’Ã˜Â§ Ã˜Â±Ã™â€¦Ã˜Â² Ã˜Â¹Ã˜Â¨Ã™Ë†Ã˜Â± Ã˜Â§Ã˜Â´Ã˜ÂªÃ˜Â¨Ã˜Â§Ã™â€¡ Ã˜Â§Ã˜Â³Ã˜Âª"
            ], 401);
        }

        $user = Auth::user();
        $token = $user->createToken("api-token")->plainTextToken;

        return response()->json([
            "message" => "Ã™Ë†Ã˜Â±Ã™Ë†Ã˜Â¯ Ã™â€¦Ã™Ë†Ã™ÂÃ™â€šÃ›Å’Ã˜ÂªÃ¢â‚¬Å’Ã˜Â¢Ã™â€¦Ã›Å’Ã˜Â²",
            "token" => $token,
            "user" => [
                "id" => $user->id,
                "name" => $user->name,
                "email" => $user->email,
                "role" => $user->role ?? "technician"
            ]
        ]);
    }

    public function logout(Request $request)
    {
        $request->user()->currentAccessToken()->delete();
        
        return response()->json([
            "message" => "Ã˜Â®Ã˜Â±Ã™Ë†Ã˜Â¬ Ã™â€¦Ã™Ë†Ã™ÂÃ™â€šÃ›Å’Ã˜ÂªÃ¢â‚¬Å’Ã˜Â¢Ã™â€¦Ã›Å’Ã˜Â²"
        ]);
    }

    public function me(Request $request)
    {
        return response()->json([
            "user" => $request->user()
        ]);
    }
}


