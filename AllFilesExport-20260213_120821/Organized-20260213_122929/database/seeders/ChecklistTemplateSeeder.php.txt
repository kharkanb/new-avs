============================================================
 فایل: ChecklistTemplateSeeder.php (پوشه: database\seeders)
============================================================

تعداد نسخه‌ها: 6

----------------------------------------
 نسخه 1 : ChecklistTemplateSeeder (2).php
----------------------------------------
<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class ChecklistTemplateSeeder extends Seeder
{
    public function run(): void
    {
        $checklists = config("equipment_checklists", []);
        
        foreach ($checklists as $typeName => $items) {
            $type = DB::table("equipment_types")->where("name", $typeName)->first();
            
            if ($type) {
                $index = 1;
                foreach ($items as $item) {
                    DB::table("checklist_templates")->updateOrInsert(
                        [
                            "equipment_type_id" => $type->id,
                            "item_index" => $index
                        ],
                        [
                            "item_text" => $item,
                            "created_at" => now(),
                            "updated_at" => now()
                        ]
                    );
                    $index++;
                }
            }
        }
    }
}


----------------------------------------
 نسخه 2 : ChecklistTemplateSeeder (3).php
----------------------------------------
<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class ChecklistTemplateSeeder extends Seeder
{
    public function run(): void
    {
        $types = DB::table("main_equipment_types")->get();
        
        foreach ($types as $type) {
            DB::table("checklist_templates")->updateOrInsert(
                [
                    "equipment_type_id" => $type->id,
                    "title" => "Ú†Ú©â€ŒÙ„ÛŒØ³Øª Ø¨Ø§Ø²Ø¯ÛŒØ¯ " . $type->name
                ],
                [
                    "description" => "Ú†Ú©â€ŒÙ„ÛŒØ³Øª Ø§Ø³ØªØ§Ù†Ø¯Ø§Ø±Ø¯ Ø¨Ø§Ø²Ø¯ÛŒØ¯ Ø¯ÙˆØ±Ù‡â€ŒØ§ÛŒ",
                    "created_at" => now(),
                    "updated_at" => now()
                ]
            );
        }
    }
}


----------------------------------------
 نسخه 3 : ChecklistTemplateSeeder (4).php
----------------------------------------
<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class ChecklistTemplateSeeder extends Seeder
{
    public function run(): void
    {
        $checklists = config("equipment_checklists", []);
        
        foreach ($checklists as $typeName => $items) {
            $type = DB::table("equipment_types")->where("name", $typeName)->first();
            
            if ($type) {
                $index = 1;
                foreach ($items as $item) {
                    DB::table("checklist_templates")->updateOrInsert(
                        [
                            "equipment_type_id" => $type->id,
                            "item_index" => $index
                        ],
                        [
                            "item_text" => $item,
                            "created_at" => now(),
                            "updated_at" => now()
                        ]
                    );
                    $index++;
                }
            }
        }
    }
}


----------------------------------------
 نسخه 4 : ChecklistTemplateSeeder (5).php
----------------------------------------
<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class ChecklistTemplateSeeder extends Seeder
{
    public function run(): void
    {
        $types = DB::table("main_equipment_types")->get();
        
        foreach ($types as $type) {
            DB::table("checklist_templates")->updateOrInsert(
                [
                    "equipment_type_id" => $type->id,
                    "title" => "Ú†Ú©â€ŒÙ„ÛŒØ³Øª Ø¨Ø§Ø²Ø¯ÛŒØ¯ " . $type->name
                ],
                [
                    "description" => "Ú†Ú©â€ŒÙ„ÛŒØ³Øª Ø§Ø³ØªØ§Ù†Ø¯Ø§Ø±Ø¯ Ø¨Ø§Ø²Ø¯ÛŒØ¯ Ø¯ÙˆØ±Ù‡â€ŒØ§ÛŒ",
                    "created_at" => now(),
                    "updated_at" => now()
                ]
            );
        }
    }
}


----------------------------------------
 نسخه 5 : ChecklistTemplateSeeder (6).php
----------------------------------------
<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class ChecklistTemplateSeeder extends Seeder
{
    public function run(): void
    {
        $checklists = config("equipment_checklists", []);
        
        foreach ($checklists as $typeName => $items) {
            $type = DB::table("equipment_types")->where("name", $typeName)->first();
            
            if ($type) {
                $index = 1;
                foreach ($items as $item) {
                    DB::table("checklist_templates")->updateOrInsert(
                        [
                            "equipment_type_id" => $type->id,
                            "item_index" => $index
                        ],
                        [
                            "item_text" => $item,
                            "created_at" => now(),
                            "updated_at" => now()
                        ]
                    );
                    $index++;
                }
            }
        }
    }
}


----------------------------------------
 نسخه 6 : ChecklistTemplateSeeder.php
----------------------------------------
<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class ChecklistTemplateSeeder extends Seeder
{
    public function run(): void
    {
        $checklists = config("equipment_checklists", []);
        
        foreach ($checklists as $typeName => $items) {
            $type = DB::table("equipment_types")->where("name", $typeName)->first();
            
            if ($type) {
                $index = 1;
                foreach ($items as $item) {
                    DB::table("checklist_templates")->updateOrInsert(
                        [
                            "equipment_type_id" => $type->id,
                            "item_index" => $index
                        ],
                        [
                            "item_text" => $item,
                            "created_at" => now(),
                            "updated_at" => now()
                        ]
                    );
                    $index++;
                }
            }
        }
    }
}


